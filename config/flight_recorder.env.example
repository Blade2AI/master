# Flight Recorder – Environment Example (DO NOT COMMIT REAL VALUES)

# Node identity (pseudonymous, no personal data)
SOVEREIGN_NODE_ID=node-uk-01

# Signing key identifier (NOT the private key itself)
SOVEREIGN_SIGNING_KEY_ID=sovereign-signing-key-node-uk-01

# Bundlr / Arweave
# Private key: keep in OS key store / secure vault, never in repo.
BUNDLR_PRIVATE_KEY_PATH=C:\Keys\bundlr-node-uk-01.json
BUNDLR_NODE_URL=https://node1.bundlr.network
BUNDLR_CURRENCY=matic

# nft.storage (or replacement IPFS pinning service)
NFT_STORAGE_API_ENDPOINT=https://api.nft.storage/upload
NFT_STORAGE_API_KEY=REPLACE_ME

# Environment tag
FLIGHT_RECORDER_ENV=proof   # dev | staging | prod | proof
